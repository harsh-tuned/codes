#include<stdio.h>
int factorial(int a){
if (a==0||a ==1){
    return ;
 }
else{
    return a* factorial(a-1);
 }
} 
int main(){
    int a;
    printf("Enter the number whose factorial you need to find: ");
    scanf("%d",&a);
    printf("%d", factorial(a));
}
